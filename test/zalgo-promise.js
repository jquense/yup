// import Promise from 'promise/src/es6-extensions';

// import ZalgoPromise from '../src/util/ZalgoPromise';

// require('promises-aplus-tests')({
//     resolved: (v) => ZalgoPromise.resolve(v),
//     rejected: (reason) => ZalgoPromise.rejec(reason),
//     deferred: () => {
//       let d = {};
//       d.promise = new ZalgoPromise((y, n) => {
//         d.resolve = y
//         d.reject = n
//       })
//     }
//   });
// });
